   <article class="post">
      <div class="postTitle">
        <strong> {{title}}</strong> by {{user.username}} on {{format_date createdAt}} <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
      </div>
      <div class="meta">
        {{content}}
        
        </div>
    </article>